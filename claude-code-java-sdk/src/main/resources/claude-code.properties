# Claude Code Java SDK 默认配置

# CLI配置
cli.path=claude-code
cli.enabled=false
timeout.seconds=600
max.retries=3

# 日志配置
logging.enabled=true

# 认证配置
# api.key=your-api-key-here
# base.url=https://api.anthropic.com

# 环境变量
# env.claude_project_name=default
# env.claude_environment=production
